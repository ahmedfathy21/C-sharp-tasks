# Ignore everything by default
*

# Allow .cs files
!*.cs

# Allow the .gitignore file itself
!.gitignore

# Allow directories (needed to traverse and find .cs files)
!*/

# Exclude specific directories (e.g., bin/, obj/, etc.)
[Bb]in/
[Oo]bj/